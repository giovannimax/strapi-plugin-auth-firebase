{
  "routes": [
    {
      "method": "GET",
      "path": "/",
      "handler": "AuthFirebase.index",
      "config": {
        "policies": [
        ]
      }
    },
    {
      "method": "GET",
      "path": "/verify",
      "handler": "AuthFirebase.verify",
      "config": {
      }
    },
    {
      "method": "GET",
      "path": "/me",
      "handler": "AuthFirebase.me",
      "config": {
      }
    },
    {
      "method": "PUT",
      "path": "/me/:id",
      "handler": "AuthFirebase.update",
      "config": {
      }
    }
  ]
}
